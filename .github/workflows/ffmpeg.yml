name: Build FFmpegKit (iOS) - Export ZIP

on:
  workflow_dispatch:
    inputs:
      enable_simulator:
        description: 'Include iOS Simulator in XCFramework'
        type: boolean
        required: true
        default: true

jobs:
  build-ios:
    runs-on: macos-latest
    timeout-minutes: 360

    steps:
      - name: Checkout Source Code
        run: git clone --depth=1 --recurse https://github.com/arthenica/ffmpeg-kit-next ffmpeg

      - name: Install Build Tools & Dependencies
        run: |
          brew update
          brew install autoconf automake libtool pkg-config cmake nasm yasm meson ninja

      - name: Setup Permissions
        run: |
          cd ffmpeg
          chmod +x apple.sh
          find scripts -type f -name "*.sh" -exec chmod +x {} + 2>/dev/null || true

      - name: Build FFmpegKit for iOS
        run: |
          cd ffmpeg
          # กำหนด Target หลักสำหรับเครื่องจริง (iOS arm64)
          TARGET_FLAGS="--target-ios-arm64"

          # หากเลือกเปิด Simulator ให้เพิ่ม Target Simulator ทั้ง arm64 และ x86_64
          if [ "${{ github.event.inputs.include_simulator }}" = "true" ]; then
            TARGET_FLAGS="$TARGET_FLAGS --target-ios-arm64-simulator --target-ios-x86-64-simulator"
          fi

          ./apple.sh \
            $TARGET_FLAGS \
            --xcframework \
            --enable-gpl \
            --enable-https \
            --enable-dav1d \
            --enable-libvpx \
            --enable-opus \
            --enable-libx264 \
            --enable-lame \
            --enable-libwebp \
            --enable-libass \
            --enable-freetype \
            --enable-fribidi \
            --enable-fontconfig

      - name: Compress XCFramework to ZIP
        run: |
          # ใช้แฟลก -y เพื่อรักษา Symbolic Links ใน Framework ให้สมบูรณ์
          zip -r -y ffmpegkit-ios-xcframework.zip bundle-apple-xcframework

      - name: Upload ZIP Artifact
        uses: actions/upload-artifact@v4
        with:
          name: ffmpegkit-ios-xcframework-zip
          path: ffmpegkit-ios-xcframework.zip
          retention-days: 7
